mixxx-ci.schnabel.dev {
	# Prometheus scrapes the monitor locally, no need to expose it.
	@metrics path /metrics
	respond @metrics 404
	reverse_proxy 127.0.0.1:8765
	log {
		output file /var/log/caddy/access.log
	}
}
